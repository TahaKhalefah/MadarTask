package com.itigraduationteam.madartask.utils

object Constant {
    const val DATABASE_NAME="user.db"
    const val REG_STRING= "(?:\\s*[a-zA-Z]+){1,3}"
    const val REG_AGE= "-?\\d+"
    const val REG_GENDER = "^M(ale)?$|^F(emale)?$"

}